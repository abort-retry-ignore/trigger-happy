{
  "name": "trigger-happy",
  "version": "1.0.0",
  "private": true,
  "description": "Crosshair overlay Chrome extension for browser FPS games. Zero dependencies, no build step.",
  "scripts": {
    "icons": "node scripts/gen-icons.js",
    "zip": "git archive --format=zip --prefix=trigger-happy/ -o trigger-happy-1.0.0.zip HEAD"
  }
}